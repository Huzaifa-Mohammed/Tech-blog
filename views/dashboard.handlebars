<section>
  <a  href="/"  class="button" > &laquo;back to home</a>
</section>

    <div class="section-title">
        {{!-- <a href="/dashboard/create/"><button id="create-post" class="button">create post</button></a> --}}
        <h2 class=""> your posts</h2><hr>
{{#if posts.length}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}
            {{!-- <a href="/dashboard/edit/{{post.id}}"><button class="button">edit post</button></a> --}}
        </div>

    {{/each}}

{{else}}
<h4 class="">No post yet</h4>

{{/if}}

{{!-- <script src="/javascript/add-post.js"></script> --}}